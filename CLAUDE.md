# CLAUDE.md — context for Claude Code sessions on this repo

This file exists so a fresh Claude Code session has full context immediately. Read `GAIUS_MASTERPLAN.md` and `GAIUS_ROADMAP.md` in full before doing anything else — this file is a pointer/summary, not a replacement for them.

## What this project is

**Gaius**: an open-source reimplementation of *Caesar* (Impressions Games, 1992/93 DOS), in the spirit of Julius/Augustus for Caesar III. Sibling project to **IGDK** (Impression Games Dev Kit, a modding IDE) and **IGA** (Impressions Games Archive, a reference hub) — those live in sibling directories, not nested inside this one, and vice versa (avoids `CLAUDE.md` context bleed across projects, per prior convention established for that project family).

## Critical rule before touching anything

**No original game assets ever get committed to this repo.** See `GAIUS_MASTERPLAN.md` section 3 (IP posture). Every test/tool that needs a real Caesar file reads it from a directory named by the `GAIUS_TEST_ASSETS` environment variable, which points at the developer's own legally-obtained copy (e.g. from GOG). Never add `.VPX`/`.PL8`/`.P32`/`.256`/`EMPIRE2.*`/`.SAV`/`CSR.EXE` files to git — `.gitignore` already blocks the common ones, but check before any `git add`.

## Current state (as of 2026-09-15)

**Phases 0-7 are complete: the simulation is validated against the original game, a month reproduces six consecutive real saves exactly, and `gaius_viewer` plays the single-player loop from a save -- the city, the province with its battles, the Forum, the maps panel, promotion to a new province and dismissal (findings section 32).** Real save files first arrived on 2026-09-12 -- four saves of one city from a real play session -- and changed a lot: they proved Phase 2's round trip against the engine, exposed wrong parameters throughout Phase 3, and overturned Phase 4's premise. `systems::service` was re-transcribed straight from the disassembly and reproduces the engine's saved A2C4 coverage and C9D4 bits **10000/10000 cells in all seventeen saves**; `systems::housing` has the engine's real development pass, every development handler and the real population table; `systems::month` reproduces a whole month's tiles, records, population and every land-value cell across six saves a month or less apart (findings section 24); `systems::economy` (2026-09-14) charges construction, collects both taxes and settles the year's accounts, reproducing every save's last year exactly (findings section 25); and `systems::military` (2026-09-14) recruits the Legion each year and assigns its Centuries to Cohorts, reproducing every save's regulars, irregulars and Cohort (findings section 26). Read `docs/CAESAR_CONSTRUCTION_DISPATCH_FINDINGS.md` sections 15, 16, 24, 25 and 26 before touching `systems/`.

- **Real saves (2026-09-12).** Fixtures live in `GAIUS_TEST_ASSETS/gaius_test_saves/` (`CAESARXX`, `WX`, `VX`, `UX.SAV`, in play order; a second session on 2026-09-14 added `CAESARXW`-`XQ`, a new city with reservoirs, working fountains and grades up to `0xD2`, all a year or more apart -- findings section 23) -- outside the game's own save directory so replaying the original can't overwrite them, and never in the repo. They proved `model::load`/`serialize` byte-identical against engine-written files; every Phase 5 footprint as a clean rectangle of its transcribed size, which confirms the row-major grid; and the save *writer*'s real global-word order. `global_words_128` is not a descending DS range: the old formula was wrong for 107 of 128 words. The exact table is `formats::save::kGlobalWordDsAddress` (treasury = `DS:0x6CA2` = save+`0x3E`).
- **`systems::service` re-transcribed from the disassembly (2026-09-13)**, proven by `test_save_corpus_simulation`. The corpus tables it had been built from were wrong in several ways. Five handlers had wrong parameters *and* names (`0xEE` is Prefecture, `0xEF` Barracks, `0xF3` Heavy Industry, `0xF4` Market, `0xEB` Oracle). Coverage ceilings aren't a universal 31, and they act as a **per-cell running minimum** (`ServiceState::coverage_ceiling`). The land-value propagator has **no floor** -- the -8..+50 clamp is a separate per-cell routine, `evolve_land_value`. Tiles `0x3C-0x3F` have no land-value effect; temples v1-3 do apply coverage; and nine handlers existed that Gaius lacked (housing tiers, temple stages, negative-coverage tiles). `tools/sim_check <save>` prints the cell-by-cell comparison with per-quarter and per-tile breakdowns.
- **Two adjacent tile tables, not one.** The monthly service scan (`0x2BBBB`) dispatches `DS:153A` for tiles above `0x35`; the housing development pass (`0x294CF`) dispatches `DS:1212` for tiles `0xC8` and up. `DS:1212`'s entries for `0xCA`-`0xFF` are the memory earlier RE read as "`DS:153A` tiles `0x00`-`0x35`" -- so `0x297AC` is housing tile `0xCA`'s handler, not dead tile-`0x00` code, an error this file briefly repeated before findings section 16 corrected it. Tiles `0x00`-`0x35` are still never simulated; in real saves they are static terrain.
- **The month is 106 steps.** Steps 0-99 run the housing pass on one row each; steps 100-105 reset, compute population and water, and run the four service scans; then the calendar advances (month `DS:0x6C1C`, year `DS:0x6C32`). Houses react to last month's services.
- **Housing (2026-09-13).** Grades `0xC8`-`0xD7` promote and demote on coverage, service bits (water `01`, `02`, market `08`, bath houses `04`, school/hospital `40`, entertainment `80`) and population units, merging into pairs, 2x2 and 3x3 houses. Population is 4x the per-cell table at `3496:007E`. (The RNG, the fountain pipe trace, the rioter that `land_value_allows` spawns and the `0xA8`-`0xB1` burning tiles came later: `systems::month`, `service::apply_water`, `systems::actors`, findings section 21.)
- **Forum, Workshop, water, economy (2026-09-13).** Tiles `0xE0`-`0xE7` are the Forum's eight grades (the corpus called them temples; `service::apply_forum`), `0xF5`/`0xF6` are workshops and `0xEF` barracks, each with a record table the save keeps (`table_480`/`table_720`/`table_120`) -- `construction::place_forum`/`place_workshop`, and the `model::CityState` overload of `clear_area` removes records on demolition. The water pass is exact, with the fountain pipe tracer (`service::fountain_supply`). `month::run_economy` transcribes step 101's four economy routines, which set the housing coverage and land-value growth bases; recomputed from each real save's inputs it reproduces all five outputs in all four saves. Findings section 19.
- **Phase 5, six passes, same day — SOLVED.** Started by finding `CAESAR_CONSTRUCTION_RE_v1.md`/`v2.md` (unread until now), which corrected Phase 3's building-handler tile IDs (`0xDF`-`0xF5`, not `0x43`-`0x50`). Then direct `capstone` disassembly of the real US-build `CSR.EXE`. Passes 2-3 ruled out five search strategies; **pass 4-5 diagnosed why they all failed** — pointer tables here store **far** pointers (offset + segment), so every search had been looking for the wrong shape of value — and proved the fix by extracting the complete real 256-entry `DS:153A` simulation-dispatch table (verified 22-for-22 against independently-published data), which revealed **`v2`'s own published tile IDs for every civic building were off by exactly one** (fixed in `systems/service.hpp`/`.cpp` — temple variant 1 is really `0xE0`/`0xE1`, not `0xDF`/`0xE0`). **Pass 6 generalized that into a whole-image scan for every far-pointer table** (runs of ≥8 entries sharing a segment word), which found the construction dispatcher: **44 entries at flat `0x75ACC`, segment `0x11C6`, DS offset `0x127C`**, dispatched by `lcall [bx + 0x127c]` with `bx = command_id * 4`. From it: definitive command IDs (~30 toolbar handlers assign them as literals), per-command seed tiles and footprints for 17 commands, and the shared terrain gate (`0x1D <= existing tile <= 0x35`). Full derivation in `docs/CAESAR_CONSTRUCTION_DISPATCH_FINDINGS.md` sections 1-14, including all the dead ends — the failed passes are kept because the reason they failed is the transferable lesson.
- `systems::construction` (Phase 5): `placement_spec`, `can_place`, `place` — real seed tiles, real footprints, real terrain gate. The four drag-built commands (Road/Wall/Plaza/Clear Area) now have their own functions, transcribed with their neighbour pattern table and validated by rebuilding every real save's road network (findings section 18); `place()` still **deliberately refuses** them and the two variant-selected ones (Forum/Workshop — footprint known, seed tile not decoded) rather than inventing values. Costs are charged since 2026-09-14 (`systems::economy`).
- Build mode in `gaius_viewer`: Tab / gamepad X cycles a tool ring, left-click / tap / gamepad A places, all through the one `platform::input` `Select` command. Mutates a live `model::CityState` and re-renders it. Headless hook: `--test-build T X Y`. The toolbar's label row shows current funds next to the tool name when it fits the panel (2026-09-14); a drag-built command (Road/Wall/Plaza/Clear Area) can be cancelled the original's way -- right button while the left is still held -- which restores every cell the drag touched and refunds its cost (`platform::CommandType::CancelDrag`).
- `systems::housing` (Phase 4): `develop_row` (the engine's row pass), `develop_building` (every `DS:1212` handler, returning the column skip), `population_units`/`population` (the real table), and `land_value_allows` (grid effects only). The RNG-driven input is supplied by the caller through `DevelopmentContext`.
- Save-file heatmap viewer: built (`apps/viewer/save_view.hpp`, wired into `gaius_viewer`); originally verified against a synthetic `SaveFile`, and it opens the real saves.
- Android NDK toolchain: built and verified end-to-end on an emulator (`android/`, see `android/README.md`) — a real gap in the original handoff's environment, not a permanent blocker; this machine already had Android Studio/SDK installed.
- `model::CityState` (Phase 2): typed `CityMap`/`Actor[70]`/reused `EmpireMap`; `load()`/`serialize()` round-trip byte-identical against all seventeen real saves. For a city actor's position use `packed_xy`, not `raw_x/raw_y` -- see `model/city_state.hpp`.

Still open: the Android build only targets the `x86_64` emulator ABI, not a real device. (The other long-standing gap -- no real `.SAV` file -- closed on 2026-09-12.)

Build and test to confirm the baseline before starting new work:

```sh
cmake -S . -B build -DCMAKE_BUILD_TYPE=RelWithDebInfo
cmake --build build -j
GAIUS_TEST_ASSETS=/path/to/your/caesar/files ./build/gaius_tests
```

Expect 3429/3429 checks passing and 0 skips with `GAIUS_TEST_ASSETS` set (including its `gaius_test_saves/` and `gaius_test_screens/` folders). Without assets the corpus tests skip rather than fail, which is expected. If this doesn't pass cleanly on a fresh clone, something regressed -- investigate before adding new code.

### What's implemented

- `formats/` — VPX, P32, .256, PL8, EMPIRE2, SAV (read-only), EXEPACK decoders. All tested against real files where available; see `docs/FORMATS.md` for exact status per format, including novel RE findings made while implementing (PL8 container header, PL8 trailing-placeholder-frame pattern) and — added in Phase 5 — the old sprite gap, **closed 2026-09-13 by real DOSBox screenshots** (`GAIUS_TEST_ASSETS/gaius_test_screens/`, never in the repo): PL8 pixels are **four interleaved streams** like VPX (the original row-major decoder was wrong; frames now match the screenshots pixel-exact), the city palette is **`SHADE.256`** (all 256 entries), and `FONT1.PL8` decodes to a legible font. The same screenshots show the game loads `.P32` as nibble × 4, and `formats::p32` now does too (it used `n * 17`); `EMAP2.VPX` matches the map screenshot outside the game's overlay. `.256` is traced in `CSR.EXE`: the file goes untouched to the VGA DAC.
- `model/` — `city_state.hpp`/`.cpp`: `CityState`/`CityMap`/`Actor` (Layer 2, Phase 2). Reshapes a loaded `SaveFile` into typed fields for the parts of the save format that are understood (5 city grids, actor table, embedded EMPIRE2 map); the other 13 of 20 blocks are carried as byte vectors, and what each holds is identified from the save writer (dispatch findings section 22) -- code reads them through `model::global_word` and the record offsets.
- `systems/` -- Layer 3. `service.hpp`/`.cpp`: **transcribed directly from the disassembly and validated cell-for-cell against real saves** -- read its header before changing anything. The three propagators (`apply_coverage` with the running-minimum ceiling, the floorless `apply_land_value`, `apply_flags`), `reset_tick`, `derive_network_flags`, `evolve_land_value`, `apply_water`, `rebuild_services` (one month-end: reset, water, full dispatch), the confidence-labeled `kC9D4BitTable`, every `DS:153A` handler, and `dispatch_tile()` with the engine's <= `0x35` gate. `housing.hpp`/`.cpp`: see above. `month.hpp`/`.cpp` (2026-09-13): the simulation clock -- `run_step`/`run_month` in the engine dispatcher's order, the calendar, and the exact random number generator (`Random`); read its header for what a month does and doesn't yet include. `actors.hpp`/`.cpp` (2026-09-13): the walkers -- the allocator, the per-tick update, movement around obstacles, the city behaviour states and the forum/workshop/barracks spawners (dispatch findings section 20); `run_step` on a whole save ticks them. `construction.hpp`/`.cpp` (Phase 5): the 34-entry command table plus `placement_spec`/`can_place`/`place`; `place` writes each footprint cell's part index (`4*dy + dx`) into `7BB4`, as the engine does. Six of the 23 placing commands are `DragAutoTiled`/`VariantSelected`, and `place` returns false for them by design. `economy.hpp`/`.cpp` (2026-09-14): the treasury -- construction costs, the population and industrial taxes, and the year's settlement with operating costs and the tribute to Rome; `run_step` runs it when the year turns, and it reproduces every save's last year of accounts (findings section 25). `military.hpp`/`.cpp` (2026-09-14): the Legion -- regular Centuries from the army wages, irregular from population and conscription, and their yearly assignment to the Cohorts (type-13 actors); `run_step` runs it after the histories (findings section 26).
- `render/` — `city_render.hpp`/`.cpp` (2026-09-13): draws the city view exactly as the original does, from `FIXTS.PL8`/`HOUSES.PL8`/`HOUSES2.PL8` and `SHADE.256`, into an indexed image (no SDL). Transcribed from the engine's draw loop and checked pixel-exact against DOSBox captures -- read `docs/CAESAR_CITY_RENDERER_FINDINGS.md` first. `gaius_viewer` shows it as the first save view when it finds the game's files (`--assets`, or beside the save). It also draws a save's walkers (`MOREMEN.PL8`, from the actor table at `DS:0x5D84`) between tile rows, as the engine does -- findings section 8. The save writer's address map for every save block is in `docs/FORMATS.md`.
- `platform/` — `window.cpp` (SDL2, logical-resolution framebuffer + letterboxing, windowed/borderless/fullscreen), `input.cpp` (unified mouse/touch/gamepad command stream), `paths.cpp` (per-OS pref path via SDL).
- `ui/` — Phase 5's toolbar, and deliberately free of both SDL and `apps/` (pure geometry plus pixel writes into an RGB24 buffer, so layout and hit-testing are headlessly testable). `metrics.hpp`/`.cpp`: four breakpoints, every size derived from one `scale`; the original's measured numbers (`kOriginalPanelH` = 24, `kOriginalIconPx` = 16) live here with provenance comments. `breakpoint_for()` takes a `has_touch` flag rather than guessing device class from resolution — an earlier size-only version classified the viewer's own 960×600 desktop window as a handheld. `toolbar.hpp`/`.cpp`: layout, hit-testing and rendering, where `render()` and `hit_test()` both go through one `button(i)` so a drawn button and its touch target cannot drift apart. Button icons are generated from `systems::construction::placement_spec` — the building's real footprint in its real map colour — rather than invented pictograms. `font.hpp`/`.cpp`: a 5×7 bitmap font that is **explicitly a placeholder, not the game's font** -- the fallback when the user's game files aren't available. `game_font.hpp`/`.cpp` (2026-09-13): the game's own `FONT1.PL8` text, drawn through the engine's `DS:0F64` character table and matched pixel-exact against a capture; the toolbar label uses it when the viewer finds the game's files.
- `apps/viewer/` — `gaius_viewer`, an interactive pannable/zoomable viewer that handles both EMPIRE2 scenarios and `.SAV` files (dispatched by exact file size), plus `apps/android_hello/` — a minimal platform-layer smoke test for the Android target (not a viewer port, see `android/README.md`).
- `tools/` -- `dump_vpx`, `dump_pl8`, `empire_view`, `save_inspect` (block table, global words with their true DS addresses, actor table), `bindiff_exe`, `sim_check` (reruns one simulation pass over a real save and compares it with the layers the engine saved), `month_check` (finds the steps between two saves of one session and checks `run_step` reproduces the later one), and `render_city` (draws a save's city with the game's sprites to a PNG).
- `tests/test_formats.cpp` — dependency-free test harness (no framework), corpus tests skip cleanly without `GAIUS_TEST_ASSETS`.

### Notable RE findings made while building this (not just implementing already-known specs)

Addenda in `docs/` document genuine reverse-engineering work done as a side effect of implementation, not just coding against a finished spec:

- `docs/CAESAR_EXEPACK_AND_STRINGS_FINDINGS.md` — the EXEPACK decompression algorithm was fully derived from disassembling the actual embedded stub (`ndisasm`), since the main RE corpus only vaguely described it. Validated against two independent real `CSR.EXE` builds. Also documents a large set of previously-unextracted game text (full promotion-rank ladder, province names, cohort status words) found via `bindiff_exe`.
- `docs/CAESAR_GOG_BUILD_FINDINGS.md` — findings from a GOG digital distribution package (a second, international/multi-language `CSR.EXE` build exists; `COHORT.CSR` resolved as a runtime handoff artifact, not shipped content; a concrete entry point for the battle-resolution system was found via `CAESAR.BAT`'s launch loop).
- `docs/CAESAR_CONSTRUCTION_DISPATCH_FINDINGS.md` (Phase 5, 16 sections) — direct `capstone`-based disassembly of the decompressed US-build image, and the largest single block of novel RE this project has produced. **Both dispatch tables are now fully recovered**: `DS:153A` (flat `0x75D8A`, simulation — tile ID → per-tick behaviour) and `DS:127C` (flat `0x75ACC`, construction — command ID → placement handler). Also: the 34-entry command string table byte-exact, per-command seed tiles/footprints/terrain gate, the `0x36`-`0x40` auto-tiling mechanism traced deeper than `v1`/`v2` had, an off-by-one correction to `v2`'s entire published tile-ID table, three building re-identifications (`0xEB` Oracle, `0xEC` School, `0xEE` Prefecture), and the DS base (`0x74850`) and main simulation-tick dispatcher (`0x293A2`) found as byproducts. **The general technique is the reusable part**: scan for runs of ≥8 consecutive 4-byte entries sharing a segment word to find far-pointer tables, then `flat = segment*16 + offset + DS_base_correction`. Sections 3-6 keep every ruled-out approach on the record. **Section 15 (2026-09-13)** reads the service layer directly: the phased tick, the <= `0x35` scan gate, the three propagators, every handler's parameters in one table, housing located at `0xC8-0xD7`, and the 10000/10000 real-save proof. **Section 16** reads housing: the `DS:1212` table and the correction it forces to section 15, the 106-step month, population and water, and every development handler.
- `docs/CAESAR_CITY_RENDERER_FINDINGS.md` (2026-09-13) — how the city view is drawn: which sheet sits in which segment, tile → frame rules, the `3496:14B2` building metrics table, per-cell sprite slices and the rows above a footprint, and why `.PL8` pixels are four interleaved streams (they're unchained-VGA planes). Proven against real DOSBox captures.

**These should eventually be folded into the main RE corpus** (`CAESAR_REVERSE_ENGINEERING_COMPLETE.md`, maintained outside this repo) — they haven't been merged there yet as of this handoff.

## Next up

1. **Done (2026-09-13): the month driver and the engine's RNG.** `systems::month` steps a month the way the engine's dispatcher does (findings section 17), with the exact random number generator, and `gaius_viewer` runs it (a month every 2 s; Space / gamepad Y pauses). Still unmodeled inside a month: the step-105, 18-month and yearly routines (ratings, messages, province events), and the random draws of frames between steps at slower game speeds. Fire, collapse and road wear (the step-105 event roll, the scan counters it rolls against, and the burning tiles) are in since 2026-09-13 -- findings section 21; the scan counts reproduce all four saves' published words.
2. **Done (2026-09-14): a month against the original.** Six saves continuing `CAESARUX` a month or less apart (`BAESARUX`...`GAESARUX`). The saves hold no step counter, but the record timers pin where each sits in its month (`tools/month_check`), and from there `run_step` reproduces the next save's tiles, record tables, population and every land-value cell (`test_month_consecutive_saves`, findings section 24). Still unchecked: the generator's draws and walker paths, which no save's data can pin.
3. **Done (2026-09-13): walkers in the city.** `systems::actors` (dispatch findings section 20): forum citizens spreading C9D4 `0x12`, workshop traders and their sales, barracks patrols that lower land value and chase hostiles, rioters from collapsed houses, and invaders' demolition. The viewer steps the month so they walk. Still open: the province-map actors (types 11-13, states 9-14) and what sends an invasion (`0x2D891`, `0x2D6F4`) -- Phases 6-7 -- and two saves a few steps apart to check a walker's path.
4. **Done (2026-09-13): drag auto-tiling.** Road, Wall, Plaza and Clear Area are transcribed (`systems::construction::place_road` / `place_wall` / `place_plaza` / `clear_area`, findings section 18) and in the viewer's tool ring. The viewer lays them cell by cell under a mouse left-drag, and a right-click during the drag cancels it with a refund (2026-09-14). Still open around them: the runtime object tables demolition updates (`DS:5BA4`/`585C`/`5B2C`).
5. **Done (2026-09-13): Forum and Workshop** (`place_forum`/`place_workshop`, findings section 19.1). The viewer chooses the grade / goods (V, right shoulder, or tapping the selected button); the label shows the grade's cost from `3496:15A0`.
6. **Done (2026-09-13): the water network** -- the exact water pass with its pipe tracer (findings section 19.2). Validated 2026-09-14: in seven saves with reservoirs and working fountains, one rebuild reproduces the water bits, fountain tiles and fountain levels exactly.
7. **Asset pipeline:** the city view, walkers, the game font and the toolbar icons all draw from the game's own files (`render/`, `ui/game_font`, `ui::command_icon_frame` -- renderer findings sections 7-8). Water and fire animate on the engine's frame counters (renderer findings section 6). The animated buildings draw too. Forum grade and workshop goods are chosen in the toolbar (V, gamepad right shoulder, or tap the selected button again). `MINIFONT.PL1` is decoded too: a 1-bit-per-pixel PL8 (`formats::pl8::load_pl1`, `ui::load_mini_font`). Still open: the overlay map modes. Mouse left-drag now lays roads and walls cell by cell (`platform::CommandType::SelectMove`); a touch drag gesture for building is still the masterplan's open design question.
8. **Done (2026-09-14): the economy's accounts** (`systems::economy`, findings section 25). Construction costs, both taxes, operating costs, the tribute, emergency funds and donations, transcribed from the build routine and the yearly routine. Every save's funds history balances year by year, with the two outside payments explained. The province terrain's cost shift and the pleb gate came later (findings sections 25.1, 29); the ratings and promotion too (section 30).
9. **Done (2026-09-14): the Legion's recruitment** (`systems::military`, findings section 26). Regulars follow the wages at 8 Dn a Century, irregulars population x conscription, and the yearly assignment moves at most one Century of each kind into a Cohort. Still open: the auxiliaries' pleb source.
10. **Done (2026-09-14): battles** (`systems::battle`, findings section 27). The province's race and its strength against each tactic, the round's arithmetic, casualties, morale, victory, defeat and retreat. The screen itself isn't modeled; the random words are the caller's.
11. **Done (2026-09-14): the province actors** (`systems::province`, findings section 28). Barbarian armies from the 18-month spawner marching on the city, wrecking and pillaging on the way and sending invaders into the city; Cohorts halting, patrolling, attacking and going home, meeting armies in battle (`SimState::on_battle`). `actors::update` runs types 11-13 in slot order. Towns, the Imperial Highway and road wear too (`0x2E249`, `0x2E220`, `0x2E0BE`). The Fort command and the Cohort orders too (`place_fort`, `order_halt`/`patrol`/`attack`/`go_home`). Province construction too (`clear_province`, `place_province_road`, `place_highway`, `place_great_wall`, `place_great_tower`). The province view and the battle screen came later (item 15).
12. **Done (2026-09-14): the plebs** (`systems::plebs`, findings section 29). What each Tribune duty needs, welfare growing or shrinking the pleb count, the assignment (army duty's plebs are the auxiliaries), and the fire, collapse and road-wear thresholds their coverage sets for step 105's rolls -- all reproduced in every save. With it the province terrain's cost shift and the 50-pleb build gate (`economy::province_cost_shift`, `enough_plebs`).
13. **Done (2026-09-14): ratings and promotion** (`systems::administration`, findings section 30) -- Phase 7's simulation. Peace, Culture, Prosperity and Empire with their population caps, the average, promotion (requirements by rank, the next province's pick, accept or wait 9/24 years, Caesar; answered through `SimState::on_promotion`) and the yearly notice. The Forum screens and maps panel came later (item 15).
14. **Done (2026-09-15): a new game, a new province and the city's terrain** (`systems::campaign`, findings section 31). `new_game`, `start_province` (the engine's full reset, funds by rank, the Prima Cohors, the highway's entry) and `generate_city` (lakes, erosion, shore patterns -- confirmed on every save's coastline -- grass and the river). Still open: the start screen.
15. **Done (2026-09-15): the screens -- Phases 6 and 7 finished** (findings section 32). `systems::forum` transcribes the Forum's buttons; `render::render_province` draws the province view from `FIXT3.PL8`/`SPRITE2.PL8`; `ui::panel` lays out full-screen pages. `gaius_viewer` (`apps/viewer/screens.hpp`) switches between the city, the province (its toolbar, forts, Cohort orders), the maps panel and the Forum (five pages); a promotion, a battle and dismissal open their own pages and stop time, and an accepted promotion loads `EMPIRE2.0NN` and starts the new province. Headless hooks: `--screen province|maps|forum`, `--forum-tab N`, `--test-action N`, `--test-battle`, `--test-promotion`. Next: Phase 8's save writer and the start screen.

More saves remain the cheapest validation. The second session (2026-09-14) covered reservoirs, working fountains and housing grades up to `0xD2`; a third set (2026-09-14) of six saves a month or less apart validated the month itself. Some saves are written mid-month -- `CAESARXS` between steps 101 and 102, `CAESARXW` and `CAESARXQ` after houses changed -- and the corpus tests name those cases rather than skipping them.

Before starting any of these: re-check `E:\\dev_res\\ig_res` for newer RE docs.

## Working conventions established so far

- Confidence-labeling discipline carries from the RE corpus into code comments: anything built on less-than-DEFINITIVE findings should say so in a comment, not present false certainty.
- Format decoders throw on any structural inconsistency rather than guessing/silently producing garbage (see `FormatError` usage throughout `formats/`).
- Corpus tests (needing real assets) skip, never fail, when assets aren't available — keep this pattern for new tests.
- **Validate simulation work against real saves, not only against transcribed parameters.** Every handler bug fixed on 2026-09-13 passed its unit tests, because those tests encoded the same wrong corpus values; `sim_check` found them all in one run. More saves -- another scenario, a later city -- are the cheapest way to widen that net.
- Licensing: code is GPL-3.0-or-later (`LICENSE`), documentation CC-BY-SA-4.0 (`docs/LICENSE`). New source files get `// SPDX-License-Identifier: GPL-3.0-or-later` as their first line. Third-party code keeps its own licence.
- Cross-platform/QoL concerns (resolution independence, input abstraction) are architecture decisions made early (Phase 1), not deferred polish — see `GAIUS_MASTERPLAN.md` section 5a. Don't add mouse-only or fixed-resolution code paths "temporarily."

## This machine's environment (Windows dev box, set up 2026-09-08)

This repo was handed off from an environment that assumed a Unix-like toolchain (plain `cmake --build`, gcc/clang, `.vscode/` configured only for a "Linux" IntelliSense target). This machine is native Windows with no gcc/clang/ninja on PATH — here's what's actually wired up, so a fresh session doesn't have to re-discover it:

- **Compiler:** MSVC via Visual Studio 2022 Build Tools (`C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`). No vcvars/Developer Prompt needed — CMake's default generator on this machine is the Visual Studio generator, which locates `cl.exe` itself. That means it's a **multi-config** generator: pass `--config RelWithDebInfo` (or `Debug`) to `cmake --build`, and binaries land in `build/<Config>/`, not `build/`.
- **SDL2:** not present as a system library — installed via vcpkg (`C:\dev\tools\vcpkg`, package `sdl2:x64-windows`). Any configure needs `-DCMAKE_TOOLCHAIN_FILE=C:/dev/tools/vcpkg/scripts/buildsystems/vcpkg.cmake`. `.vscode/settings.json` and `.vscode/tasks.json`'s Windows-specific command overrides already do this; a bare manual `cmake` invocation from a terminal needs the flag added by hand.
- **`GAIUS_TEST_ASSETS`:** wired to `E:\dev_res\ig_res\Caesar\US` (a legally-obtained GOG copy, extracted outside this repo — see IP posture rule above). Set via `.vscode/settings.json`'s `terminal.integrated.env.windows`/`cmake.testEnvironment`, and as a fallback env override on the Windows `tasks.json` test task. **Use `Caesar/US/`, not the `Caesar/` root** — the root holds a second, newer international/multi-language `CSR.EXE` build that `docs/CAESAR_GOG_BUILD_FINDINGS.md` explicitly flags as *not yet analyzed*; `US/` is the build every existing finding and the `test_exepack_golden_decode` fixture was validated against (it checks for the `CaesarXX.sav` string specific to that build). A copy of `EMAP2_decoded.png` (the golden-image reference, not a game asset) was placed in `Caesar/US/` alongside it since the golden-image test expects it next to `EMAP2.VPX`. Real save fixtures for the corpus tests live in `Caesar/US/gaius_test_saves/`, and DOSBox screenshots from real play (sprite/palette ground truth) in `Caesar/US/gaius_test_screens/`.
- **Full verified command sequence on this machine:**
  ```powershell
  cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=C:/dev/tools/vcpkg/scripts/buildsystems/vcpkg.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo
  cmake --build build --config RelWithDebInfo -j
  $env:GAIUS_TEST_ASSETS = "E:\dev_res\ig_res\Caesar\US"
  .\build\RelWithDebInfo\gaius_tests.exe
  ```
- **Android:** this machine already had Android Studio + an SDK at `%LOCALAPPDATA%\Android\Sdk` (build-tools, platform-tools, and a `Medium_Phone` AVD were all pre-existing) — only `cmdline-tools` and the NDK (`ndk;29.0.14206865`) needed installing, both via `sdkmanager`. **Gradle needs a JDK 17 on `JAVA_HOME`** — Android Studio's bundled JBR is JDK 25, too new for this project's Gradle 8.1.1/AGP 8.1.1; installed Eclipse Temurin 17 (`C:\Program Files\Eclipse Adoptium\jdk-17.0.20.101-hotspot`) specifically for this. See `android/README.md` for the full build/run sequence.
